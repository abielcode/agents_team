# Agents Team — Phase 1 dependencies

# Anthropic SDK (Orchestrator)
anthropic>=0.40.0

# OpenAI-compatible client (Ollama gateway)
openai>=1.50.0

# FastAPI + server (Phase 2 — included now for smooth transition)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
websockets>=13.0

# Database (Phase 2)
sqlalchemy>=2.0.0
aiosqlite>=0.20.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0
httpx>=0.27.0
pyyaml>=6.0.0         # xcodegen_sync project.yml parsing
